@model IEnumerable<Projeto_final_Ti2_2018.Models.CarrinhoComprasProdutos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CarrinhoCompras.ClienteFK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Produto.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantidade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CarrinhoCompras.ClienteFK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Produto.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantidade)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDcomprasProduto }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDcomprasProduto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDcomprasProduto })
        </td>
    </tr>
}

</table>
